services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    ports:
      - "18001:3001"
    volumes:
      - uptime-kuma-data:/app/data
    labels:
      - "customization.image.slug=uptime-kuma"
    restart: unless-stopped

volumes:
  uptime-kuma-data: {}
